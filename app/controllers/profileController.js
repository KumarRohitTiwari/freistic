var profileController = (function () {
	return function ($scope, $window, $timeout, $stateParams, $http) {
        var self = this;
    }
})();
profileController.$inject = ["$scope", "$window", "$timeout", "$stateParams", "$http"];
app.controller("profileController", profileController);